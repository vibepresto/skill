interface:
  display_name: "VibePresto"
  short_description: "Manage pages and deploy static sites to VibePresto with the CLI"
  default_prompt: "Use $vibepresto to create or manage pages and bundle and deploy a static HTML/CSS/JS site to a VibePresto WordPress instance with the published `npx vibepresto` CLI."
